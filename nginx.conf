server {
  listen 80;

  location /.well-known/acme-challenge/ {
    root /srv/wbb/frontend/build;
  }
  location / {
    root /usr/share/nginx/html/;
    include /etc/nginx/mime.types;
    try_files $uri $uri/ /index.html;
  }
}