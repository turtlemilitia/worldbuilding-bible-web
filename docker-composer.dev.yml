version: "3.8"

services:
  app:
    container_name: dnd-app
    image: dnd-app-frontend-dev
    build:
      context: .
      target: development
    volumes:
      - /node-modules
      - ./src:/app/src
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      WATCHPACK_POLLING: "true"